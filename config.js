// Paste your Supabase project values here once; every page reads them.
// The anon key is meant to be public — this is the correct place for it.
//
// Local Supabase (supabase start) prints these; or copy from a cloud project's
// Project Settings -> API.
const CONFIG = {
  URL:  "PASTE_PROJECT_URL",
  ANON: "PASTE_ANON_KEY"
};
